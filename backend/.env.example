# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/riskcoins?schema=public"

# JWT Configuration (CHANGE THESE IN PRODUCTION!)
JWT_SECRET="GENERATE_A_STRONG_RANDOM_STRING_AT_LEAST_64_CHARS"
JWT_EXPIRES_IN="1h"

# Server Configuration
PORT=4000
NODE_ENV=production

# Admin Secret (CHANGE THIS IN PRODUCTION!)
ADMIN_SECRET="GENERATE_A_STRONG_RANDOM_ADMIN_SECRET_KEY"

# CORS (Set to your frontend domain in production)
FRONTEND_URL="http://localhost:3000"

# --------------------------------------------------
# EMAIL & BRANDING (configure for real SMTP)
# --------------------------------------------------
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT=587
EMAIL_USER="your-school-email@school.edu"
EMAIL_PASS="your-app-password-here"
EMAIL_FROM="your-school-email@school.edu"

# Branding (used in email subjects & bodies)
BRAND_NAME="RIS House Coins"
# BRAND_SYSTEM_NAME="RIS House Coins School System"  # optional override
